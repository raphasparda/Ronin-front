engine-strict=true
# Escopo do @raphasparda/ronin-shared no GitHub Packages, usado quando o web consome a versão
# publicada (o token nunca fica aqui: ~/.npmrc do usuário ou NODE_AUTH_TOKEN no CI).
@raphasparda:registry=https://npm.pkg.github.com
